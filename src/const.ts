export enum APIRoute {
  Courses = "/courses",
  Registration = "/registration",
  Suggestions = "/suggestions",
  Themes = "/themes",
  Trends = "/trends",
  Login = "/login",
  Logout = "/logout",
}

export enum HttpCode {
  BadRequest = 400,
  Unauthorized = 401,
  NotFound = 404,
}
